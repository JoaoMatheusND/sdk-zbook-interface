/*
 * Layered on top of app.overlay for the interface.sensors.ldr.read_failure
 * scenario. Repoints ldr_adc at channel 5, past adc0's 2-channel range, so
 * adc_read() itself rejects the sequence (adc_emul_start_read's channel
 * mask check), regardless of whether adc_channel_setup() ever succeeded.
 */

&ldr_adc {
	reg = <5>;
};
